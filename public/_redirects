# The hosted admin is disabled on purpose.
#
# Curation runs locally (npm run curate, or npm run cms + npm run dev), so a
# public /admin can only ever fail to authenticate. Delete this rule and set
# `base_url` in public/admin/config.yml if you later add a GitHub OAuth proxy.
/admin/*  /404.html  404
/admin    /404.html  404
