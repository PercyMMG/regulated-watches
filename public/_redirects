# The hosted admin is live at /admin/ and authenticates with GitHub through
# the OAuth proxy in functions/api/. Only accounts with write access to the
# repository can commit anything, so the login page itself is not a risk.
#
# To take it offline again, restore:
#   /admin/*  /404.html  404
#   /admin    /404.html  404
